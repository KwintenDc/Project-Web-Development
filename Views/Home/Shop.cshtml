@model IEnumerable<Project_WebDev.Models.Item>

    <nav class="navbar navbar-expand-md navbar-light bg-light">
    <div class="collapse navbar-collapse shopBar" id="navbarSupportedContent">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link active" href="#" data-type="All">All</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#" data-type="Airsoft Guns">Airsoft Guns</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#" data-type="Ammo & Supplies">Ammo & Supplies</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#" data-type="Gun Accesoires">Gun Accesoires</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#" data-type="Tactical Gear">Tactical Gear</a>
            </li>
            <li class="nav-item dropdown ms-auto">
                <a class="nav-link dropdown-toggle" href="#" id="sortDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    Sort By
                </a>
                <ul class="dropdown-menu" aria-labelledby="sortDropdown">
                    <li><a id="sortLowToHigh" class="dropdown-item" href="#">Price: Low to High</a></li>
                    <li><a id="sortHighToLow" class="dropdown-item" href="#">Price: High to Low</a></li>
                </ul>
            </li>
        </ul>
    </div>
    </nav>
    <main class="container mt-5">
        <main class="container mt-5" id="itemContainer">
            <div id="Airsoft Guns" class="itemDiv">
                <div class="row">
                    <h3>AEG Assault Rifles</h3>
                    @foreach (var item in Model.Where(i => i.Type == "AEG-Assault-Rifle"))
                    {
                        @Html.Partial("_ItemCard", item)
                    }
                </div>
                <div class="row">
                    <h3>Gas Pistols</h3>
                    @foreach (var item in Model.Where(i => i.Type == "Gas-Pistol"))
                    {
                        @Html.Partial("_ItemCard", item)
                    }
                </div>
                <div class="row">
                    <h3>HPA Guns</h3>
                    @foreach (var item in Model.Where(i => i.Type == "HPA-Gun"))
                    {
                        @Html.Partial("_ItemCard", item)
                    }
                </div>
                <div class="row">
                    <h3>Snipers</h3>
                    @foreach (var item in Model.Where(i => i.Type == "Sniper"))
                    {
                        @Html.Partial("_ItemCard", item)
                    }
                </div>
                <div class="row">
                    <h3>Shotgun</h3>
                    @foreach (var item in Model.Where(i => i.Type == "Shotgun"))
                    {
                        @Html.Partial("_ItemCard", item)
                    }
                </div>
            </div>
            <div id="Ammo & Supplies" class="itemDiv">
                <div class="row">
                    <h3>BBs</h3>
                    @foreach (var item in Model.Where(i => i.Type == "BBs"))
                    {
                        @Html.Partial("_ItemCard", item)         
                    }
                </div>
                <div class="row">
                    <h3>Gas</h3>
                    @foreach (var item in Model.Where(i => i.Type == "Gas"))
                    {
                        @Html.Partial("_ItemCard", item)
                    }
                </div>
                <div class="row">
                    <h3>Batteries</h3>
                    @foreach (var item in Model.Where(i => i.Type == "Battery"))
                    {
                        @Html.Partial("_ItemCard", item)
                    }
                </div>
            </div>
            <div id="Gun Accesoires" class="itemDiv">
                <div class="row">
                    <h3>Mags</h3>
                    @foreach (var item in Model.Where(i => i.Type == "Mags"))
                    {
                        @Html.Partial("_ItemCard", item)
                    }
                </div>
                <div class="row">
                    <h3>Optics</h3>
                    @foreach (var item in Model.Where(i => i.Type == "Optics"))
                    {
                        @Html.Partial("_ItemCard", item)
                    }
                </div>
                <div class="row">
                    <h3>Grips</h3>
                    @foreach (var item in Model.Where(i => i.Type == "Grips"))
                    {
                        @Html.Partial("_ItemCard", item)
                    }
                </div>
            </div>
            <div id="Tactical Gear" class="itemDiv">
                <div class="row">
                    <h3>Tactical Gear</h3>
                    @foreach (var item in Model.Where(i => i.Type == "Tactical Gear"))
                    {
                        @Html.Partial("_ItemCard", item)
                    }
                </div>
            </div>
        </main>
    </main>