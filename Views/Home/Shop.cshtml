@model IEnumerable<Project_WebDev.Models.Item>

<link rel="stylesheet" type="text/css" href="~/css/styles.css" />
    <nav class="navbar navbar-expand-md navbar-light bg-light">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="#" data-type="Airsoft Guns">Airsoft Guns</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#" data-type="Ammo & Supplies">Ammo & Supplies</a>
                </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" data-type="Gun Accesoires">Gun Accesoires</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" data-type="Tactical Gear">Tactical Gear</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <main class="container mt-5">
        <main class="container mt-5">
            <div>
                <div class="row">
                    @foreach (var item in Model.Where(i => i.Type == "AEG-Assault-Rifle"))
                    {
                    <div class="col-12 col-md-6 col-lg-4 mb-3">
                        <div class="card">
                            <img src="@Url.Content($"~/{item.Image}")" class="card-img-top" alt="@item.Name">
                            <div class="card-body">
                                <h5 class="card-title">@item.Name</h5>
                                    <p>@item.Brand</p>
                                <a href="#" class="btn btn-primary">Add To Cart</a>
                            </div>
                        </div>
                    </div>
                    }
                </div>
                <div class="row">
                    @foreach (var item in Model.Where(i => i.Type == "Gas-Pistol"))
                    {
                    <div class="col-12 col-md-6 col-lg-4 mb-3">
                        <div class="card">
                            <img src="@Url.Content($"~/{item.Image}")" class="card-img-top" alt="@item.Name">
                            <div class="card-body">
                                <h5 class="card-title">@item.Name</h5>
                                    <p>@item.Brand</p>
                                    <a href="#" class="btn btn-primary">Add To Cart</a>
                            </div>
                        </div>
                    </div>
                    }
                </div>
                <div class="row">
                    @foreach (var item in Model.Where(i => i.Type == "HPA-Gun"))
                    {
                    <div class="col-12 col-md-6 col-lg-4 mb-3">
                        <div class="card">
                            <img src="@Url.Content($"~/{item.Image}")" class="card-img-top" alt="@item.Name">
                            <div class="card-body">
                                <h5 class="card-title">@item.Name</h5>
                                    <p>@item.Brand</p>
                                    <a href="#" class="btn btn-primary">Add To Cart</a>
                            </div>
                        </div>
                    </div>
                    }
                </div>
                <div class="row">
                    @foreach (var item in Model.Where(i => i.Type == "Sniper"))
                    {
                    <div class="col-12 col-md-6 col-lg-4 mb-3">
                        <div class="card">
                            <img src="@Url.Content($"~/{item.Image}")" class="card-img-top" alt="@item.Name">
                            <div class="card-body">
                                <h5 class="card-title">@item.Name</h5>
                                    <p>@item.Brand</p>
                                    <a href="#" class="btn btn-primary">Add To Cart</a>
                            </div>
                        </div>
                    </div>
                    }
                </div>
                <div class="row">
                    @foreach (var item in Model.Where(i => i.Type == "Shotgun"))
                    {
                        <div class="col-12 col-md-6 col-lg-4 mb-3">
                            <div class="card">
                                <img src="@Url.Content($"~/{item.Image}")" class="card-img-top" alt="@item.Name">
                                <div class="card-body">
                                    <h5 class="card-title">@item.Name</h5>
                                        <p>@item.Brand</p>
                                        <a href="#" class="btn btn-primary">Add To Cart</a>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </main>
    </main>